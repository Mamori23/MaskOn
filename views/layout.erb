<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="main.css">
    <title>Mask On</title>
</head>
<body>

     
         <div id='header-top'> 
            <a href='/' class='link-line'>Home</a>
            <% if !session[:user_id]  %>
              <a href="/signup" class='link-line'> Signup</a>
              <a href="/login" class='link-line'> Login </a>
              <a href="/cancel" class='link-line'> Cancel </a>
            <% else %> 
  
          <% if session[:user_id] %>
              <a href="/feed" class='link-line'>Feed </a>
              <a href="/profile" class='link-line'> Profile</a>
              <a href="/logout" class='link-line'> Logout </a>
          <% end %>
          <% end %>
     </div>
 
<%= yield %>
</body>
</html>